<UserControl x:Class="GameLauncher.UserControls.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameLauncher.UserControls"
             mc:Ignorable="d" Background="Transparent"
             d:DesignHeight="800" d:DesignWidth="1280">
    <Border CornerRadius="5" Background="{StaticResource Foreground}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="2" Content="X" Click="Button_Click" Style="{StaticResource IMMCButton}"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
